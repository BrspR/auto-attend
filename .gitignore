# credentials and personal schedule — never commit these
.env
config.py

# browser cache and session data
cache/class_urls.json

# python
.venv/
__pycache__/
*.pyc
*.pyo

# runtime
scheduler.log
nohup.out
*.png
.claude/

# nima live-session debug dumps (auto-saved when a ورود button isn't found)
nima_debug_*.html

# bbb in-room debug dumps (auto-saved the first time the bot is in a live room)
bbb_debug_*.html
